import React from "react";
import "../../styles/form.css";
const Form = ({ formRef }: any) => {
  return (
    <div ref={formRef} className="combo-table-form-wrapper-side">
      test
    </div>
  );
};

export default Form;
